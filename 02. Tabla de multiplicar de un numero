#include <iostream>
using namespace std;

int main() {
    int numero;
    cout << "Tabla de que numero:";
    cin >> numero;
    for (int i = 1; i <= 12; ++i) {
        cout << numero << " * " << i << " = " << numero * i << endl;
    }
    return 0;
}
